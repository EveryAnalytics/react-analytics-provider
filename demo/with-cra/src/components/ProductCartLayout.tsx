import React from 'react';
import styled from '@emotion/styled';

function ProductCartLayout({children}: {children: React.ReactNode}) {
  return <div>{children}</div>;
}

export default ProductCartLayout;
